export default defineAppConfig({
  ui: {
    colors: {
      primary: 'primary',
      neutral: 'zinc'
    },
    button: {
      slots: {
        base: 'font-medium rounded-lg transition'
      }
    }
  }
})
